# Glance — Dashboard

Single-pane overview at `https://glance.cianfhoghlaim.ie`. Login via PocketID SSO.

Shows: Vikunja task counts, cal-diy upcoming bookings, Paperless recent uploads, Changedetection alerts, n8n workflow health, LiteLLM gateway status, RisingWave dashboard link.

Configured via `config/glance.yml`.
