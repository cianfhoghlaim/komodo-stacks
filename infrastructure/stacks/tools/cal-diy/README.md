# cal-diy — Team Scheduling

cal.com community build at `https://calcom.cianfhoghlaim.ie`. Login via PocketID SSO.

Team booking page at `/team`. Per-member pages at `/<member-slug>`.
Outbound webhooks → n8n (booking → Vikunja task with Gantt dates).

Backed by PlanetScale "bunchloch" (schema: `calcom`).
